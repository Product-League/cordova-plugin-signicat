repositories {
    flatDir {
        dirs "${projectDir}/libs"
    }
}

dependencies {
   implementation(name:'android-sdk-1.1.6', ext:'aar')
}

android {
    defaultConfig {
        manifestPlaceholders = [
            appAuthRedirectScheme: "https",
            appAuthRedirectHost: "testpilot.hollandzorg.com"
        ]
    }
}

